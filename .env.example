# === Server ===
APP_ENV=development
API_V1_STR=/api/v1
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=["http://localhost:3000"]

# === Security (demo only; replace in prod) ===
SECRET_KEY=CHANGE_ME_SUPER_SECRET
ACCESS_TOKEN_EXPIRE_MINUTES=60

# === DB ===
DATABASE_URL=sqlite+aiosqlite:///./app.db

# === Media ===
MEDIA_DIR=./uploads
