[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
markers =
    unit: fast unit tests
    integration: endpoint and database integration tests
    slow: slow tests